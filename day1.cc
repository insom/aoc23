#include <bits/stdc++.h>

#include <fstream>
#include <iostream>
#include <vector>

using namespace std;

int main() {
  std::ifstream ifs("input1");
  std::string line;

  while (ifs.good()) {
    getline(ifs, line);
  }
}
